{
  "name": "office-attendance-vercel",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build --prefix frontend",
    "test": "npm test --prefix frontend"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "exceljs": "^4.4.0",
    "pdfkit": "^0.15.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "@types/pdfkit": "^0.13.6",
    "@vercel/node": "^5.0.0",
    "typescript": "^5.6.3"
  }
}
